
import React, { Component } from 'react';
import { Image, ScrollView, Text } from 'react-native';

class DetailedSteps extends Component {
  render() {
    return (
      <ScrollView>
        <Image
          source={{uri: 'https://hoopshabit.com/wp-content/uploads/getty-images/2017/10/867977994-new-york-knicks-v-cleveland-cavaliers.jpg.jpg'}}
          style={{width: 320, height:180}}
        />
        <Text>
          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.




          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.
          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.
          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.
          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.
          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.
          On iOS, a React Native ScrollView uses a native UIScrollView.
          On Android, it uses a native ScrollView.

          On iOS, a React Native Image uses a native UIImageView.
          On Android, it uses a native ImageView.

          React Native wraps the fundamental native components, giving you
          the performance of a native app, plus the clean design of React.
        </Text>
      </ScrollView>
    );
  }
}
export default DetailedSteps;

  /*
  import React, { Component } from 'react';
  import { AppRegistry, Image,Text } from 'react-native';

  export default class Bananas extends Component {
    render() {
      let pic = {
        uri: 'https://static1.squarespace.com/static/5a4422ba914e6b6fa85c2aeb/5a5bd13b8165f5ec9ddb901f/5a5bd26471c10bca132ab716/1515967705692/Michael_Okrah.jpg'
      };
      return (
        <Image source={pic} style={{width: 200, height: 310}}/>
      //  <Text>rjwcgnasjkwfhrnciwy</Text>
      );
    }
  }







  import React, { Component } from 'react';
  import { StyleSheet ,Text, View,Button,ImageBackground} from 'react-native';





   class DetailedSteps extends Component {
     static navigationOptions = {
     title: 'Detailed Instructions/ Materials',
    };
    render() {
      return (
        <View>




        <Text> Make this into a tab with about 2 images with extremely maticulous instructios </Text>
        <Text> Refrence the react website if anything </Text>
        <Text> At the very end there should be a 'next' button that navigates to the DETAILED materials and STEP BY STEP instructions w/ Images </Text>
        <Text> MOST LIKELY WILL HAVE SEVERAL PAGES/TAB FOR INSTRUCTIONS </Text>


          <Button onPress= {() => this.props.navigation.navigate ('CaptureImage')}
            title="Next "/>

        </View>



            );
          }
        }
        const styles = StyleSheet.create({
          container:{
            flex:1,
            width:'100%',
            height:'100%'
          }
        });


    export default DetailedSteps;

  */
